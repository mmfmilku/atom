## 低代码部分
### 一、try原子处理实现优化，支持try之后add
### 二、if处理支持解析operate操作

## 交互优化


## agent部分
### 编译器语法支持
* 注解中变量赋值
* ~~throw 语句支持~~
* 泛形
* 其他关键字 switch
* 字面量：~~字符串，~~字符，Unicode字符，
数字（二进制、八进制、十进制、十六进制）~~（长整型，浮点型）~~
* 可变入参语法
* ~~new com.xxx.xxx.X() 语法~~
* 抽象方法
* 三目表达式
* 变量赋值语句作为表达式
* 数组解析
* 增强for循环，循环变量是一个表达式，现在只能解析标识符
* 解析continue,break 关键字
* try语句getAllExpression实现
* 类型全类名强转 int o2i2 = (java.lang.Integer) o + 44;

## FServer
* 请求容错处理
* 初始连接服务端与客户端协商token
* 心跳保活
* 添加长连接handle

## FRPC
* 调用异常封装
* 清除已关闭的连接
* 无需载入修改代理配置

## feature
* 重写策略组
* 执行过程数据展示
* 添加可执行控制台

## 优化
* 通过类加载器加载，支持卸载
* 展示列表不好区分是哪个环境

## 后端BUG
* agent断开，终止hook优化
* ~~代理应用重启，客户端连接异常，重写数据异常~~
* 应用重启，连接异常（服务端发送关闭连接消息，客户端不会主动读取消息）
* ~~判断连接优化~~
* 程序反编译缓存清理
* ~~偶现，启动console后，连接agent获取的类列表没有通过设置应用基础包过滤，后续反编译通信一直超时~~
* 对于内部类源码获取失败的处理
* 返回文件类型
* 调用的方法存在可变参数的情况,无法识别。
  如方法定文 (String var1, Object... var2)
  调用("var1")将识别为(String varl)的方法定义
  调用("var1","var2")将识别为(String varl,String var2)的方法定义
 * 重写transform中出现的报错,web端界面无法获取,并且界面还是展示的绿色重写中
 * 语法不支持的提醒不友好
 * 脚本化,每次修改都要先点保存再点运行,不方便
 * *打包执行的工程,脚本化执行,获取不到用户类
 * 脚本化执行结果,添加结果展示控制台
 * *web控制台,每次启动清理缓存jar

## 前端BUG
* 前端轻提示，在被弹窗层遮挡
* 前端首次载入后，未将载入按钮置灰
* 页面返回路由支持
* ~~右键菜单，未选择列表项时的处理~~
* 控制台各类文件的保存，保存类型通过列表获取

## 单元测试案例补充
* do while 语法解析与还原

测试提交